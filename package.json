{
  "name": "particle-farm-monitor",
  "scripts": {
    "build": "particle compile electron",
    "monitor": "sudo cu -s 9600 -l /dev/tty.usbmodem1411",
    "prebuild": "del-cli *.bin"
  },
  "devDependencies": {
    "del-cli": "^1.0.0"
  }
}
